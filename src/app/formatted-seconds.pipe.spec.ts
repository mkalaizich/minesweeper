import { FormattedSecondsPipe } from './formatted-seconds.pipe';

describe('FormattedSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
